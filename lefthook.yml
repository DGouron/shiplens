pre-commit:
  parallel: true
  jobs:
    - name: biome
      glob: "*.{ts,tsx}"
      run: pnpm biome check --write --no-errors-on-unmatched --files-ignore-unknown=true --staged
      stage_fixed: true
    - name: typecheck
      run: pnpm typecheck
